# @getcast/agent-kit

CLI for creating and managing Cast agents (`pnpm agent ...`). Not imported by the Cast server at runtime.

## Templates

`templates/` is gitignored — each subdirectory is an independent git repo. Glob does not recurse into them; use absolute paths to read files inside. Each template has its own README describing its purpose and customization points.
